test:
  override:
    - ./node_modules/karma/bin/karma start --single-run --reporters dots

deployment:
  nightly:
    branch: develop
    commands:
      - "STACK_ID=$AWS_NIGHTLY_STACK_ID APP_ID=$AWS_NIGHTLY_STACK_ID gulp deploy"